import {types} from './currency-purchase-actions';
const defaultState = {
};

export default function currencyPurchaseStateReducer(state = defaultState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
